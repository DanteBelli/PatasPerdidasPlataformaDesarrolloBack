import express from "express"; const app = express(); const port = 5000; app.get("/", (request, response) => { response.send("Hello World!"); }); app.listen(5000, () => { console.log(`Server is running on port ${port}`); }); 